# Paid Services: API Development

We provide professional **API development, integration, and consulting services**.  
All services are **paid**, with transparent pricing.  
Payment can be made via **Cash** or **Card**.

---

## 🛠 API Services

| Service                 | Description                                      | Price    |
| ----------------------- | ------------------------------------------------ | -------- |
| REST API Development    | Custom backend API for your app or website       | $50/hour |
| GraphQL API Development | Flexible, query-based API development            | $60/hour |
| API Integration         | Integrate third-party APIs (Payment, Maps, etc.) | $40/hour |
| API Documentation       | Professional documentation for your API          | $25      |
| API Testing & Debugging | Test and fix issues in existing APIs             | $30/hour |

---

## 🔹 Sample Code Snippets

### 1. REST API using Express.js

```javascript copy
// server.js
const express = require("express");
const app = express();
const PORT = 5000;

// Middleware
app.use(express.json());

// Sample GET API
app.get("/api/hello", (req, res) => {
  res.json({ message: "Hello from API!" });
});

// Sample POST API
app.post("/api/data", (req, res) => {
  const { name } = req.body;
  res.json({ message: `Hello, ${name}` });
});

app.listen(PORT, () => console.log(`Server running on port ${PORT}`));
```
